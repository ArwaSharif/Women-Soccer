const express = require('express');
const router = express.Router();
const favoritesCtrl = require('../../controllers/api/addFavorites');

//TO DO
// POST /api/
// adding a new fav team
// router.post('/', favoritesCtrl.update);

module.exports = router;